<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Récupération du flux ICS Skello. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      USE_EXACT_ALARM est accordée d'office sur Android 13+ aux applications dont la
      fonction principale est le rappel programmé : rien à demander à l'utilisateur.
      SCHEDULE_EXACT_ALARM ne sert que de repli sur Android 12, où elle est implicite.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Uniquement demandée si la source choisie est le calendrier de l'appareil. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      Alarme plein écran par-dessus l'écran de verrouillage quand un badgeage reste ignoré.
      Accordée d'office jusqu'à Android 13 ; à partir d'Android 14 elle est réservée aux
      applications d'appel et de réveil, et doit être accordée explicitement — ce que
      l'installation guidée propose de faire.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!--
      Permet de lister les applications lançables pour que l'utilisateur désigne
      lui-même la badgeuse, sans avoir à deviner un nom de paquet et sans recourir
      à QUERY_ALL_PACKAGES.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".SkelloBadgeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.SkelloBadge">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SkelloBadge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Alarme plein écran. excludeFromRecents et noHistory évitent qu'elle traîne dans
          les applications récentes une fois le badgeage confirmé.
        -->
        <activity
            android:name=".notify.FullScreenAlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:taskAffinity=".alarm"
            android:theme="@style/Theme.SkelloBadge" />

        <!-- Déclenché par AlarmManager via un PendingIntent de l'application elle-même. -->
        <receiver
            android:name=".schedule.ReminderReceiver"
            android:exported="false" />

        <!-- Actions « J'ai badgé » et « Dans 5 min » portées par les notifications. -->
        <receiver
            android:name=".notify.NotificationActionReceiver"
            android:exported="false" />

        <!--
          Les alarmes exactes ne survivent ni au redémarrage, ni à un changement d'heure,
          ni à une mise à jour de l'application : on replanifie systématiquement.
          Ces diffusions sont protégées — seul le système peut les émettre.
        -->
        <receiver
            android:name=".schedule.SystemEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
