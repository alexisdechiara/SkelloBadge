<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Thème système minimal : toute l'apparence est portée par Compose Material 3
      Expressive. Il ne sert qu'à cadrer la fenêtre avant le premier rendu.
    -->
    <style name="Theme.SkelloBadge" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>
</resources>
